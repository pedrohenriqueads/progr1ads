#4 - Calcule a área de um retângulo (base x altura).
base = float(input("Digite a base do retângulo: "))
altura = float(input("Digite a altura do retângulo: "))
soma = base * altura
print("A área do retângulo é:", soma)
